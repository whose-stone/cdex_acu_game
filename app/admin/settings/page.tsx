export default function AdminSettingsPage() {
  return (
    <main className="container">
      <h1>Admin — Settings</h1>
      <p>Slice G placeholder: level settings, lives limits, and per-level configuration.</p>
    </main>
  );
}
